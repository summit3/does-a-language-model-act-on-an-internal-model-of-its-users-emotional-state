# results/

Outputs only; everything here is regenerated by code in `src/` or `notebooks/`. Nothing is hand-edited.

| File | What it is | Produced by |
|---|---|---|
| `residual_norms_by_layer.csv` | L2 norm of the residual stream at the last prompt token, one row per decoder layer, for the compound-interest sanity prompt on the local model (`src/config.py:MODEL_ID`). | `src.hooks.get_residual_activations` sanity check |
| `residual_norms_by_layer.png` | Plot of the above. Sanity-check artefact: a smooth monotone-ish rise across layers means the hooks sit on the right modules and the tensor is finite. | same |

Planned (added as the project proceeds): reply CSVs per condition (`replies_<condition>.csv`), probe accuracy-by-layer tables (`probe_acc_<split>.csv`), steering dose-response tables, and the plots for the write-up.
